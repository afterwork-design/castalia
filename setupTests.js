// Optional: configure or set up a testing framework before each test.
// If Jasmine and Jest are used, all the global variables (expect, describe, it, etc.) are shared between them.
//
// If you need to configure something in Jest, you can do it here. For example:

require('@testing-library/jest-dom');